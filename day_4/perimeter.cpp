#include<iostream>
using namespace std;

int main()
{
    float length,breadth,per;
    cout<<"\n enter the length = ";
    cin>>length;
    cout<<"\n enter the breadth = ";
    cin>>breadth;

    per = (2*(length+breadth));

    cout<<" area of perimeter of rectangle is = "<<per;
}